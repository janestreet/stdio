(library
 ((name stdio)
  (public_name stdio)
  (libraries (base base.caml))
  (preprocess no_preprocessing)
  (lint (pps (ppx_js_style -check-doc-comments)))
  (js_of_ocaml ())))

(enforce_style ())
